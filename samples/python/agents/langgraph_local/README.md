# LangGraph Local Agent

A simple LangGraph agent with weather and calculator tools.

## Run

```bash
cd samples/python/agents/langgraph_local
echo "GOOGLE_API_KEY=your_key" > .env
pip install -e .
langgraph dev
```

Server runs at `http://localhost:2024`
